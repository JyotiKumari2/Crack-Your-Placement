#include<bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        ll n,q;
        cin>>n>>q;
        vector<ll>v(n);
        for(ll i=0;i<n;i++)
           cin>>v[i];
        while(q--)
        {
            ll i,k;
            
        }
    }
    return 0;
}

 